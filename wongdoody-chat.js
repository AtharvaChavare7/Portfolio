(function initSpendChat() {
  const chatRoot = document.querySelector("[data-spend-chat]");

  if (!chatRoot) {
    return;
  }

  // Chat animation paused while UI is rebuilt from Figma piece by piece.
})();
